#!/bin/sh

# FIXME: We should be looking for an encryption key

# this pack is valid for apps with a hello.txt in the root
#if [ -f $1/hello.txt ]; then
  echo "SecureSlug"
  exit 0
#else
#  exit 1
#fi
